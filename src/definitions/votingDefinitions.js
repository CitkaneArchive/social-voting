/**
 * A vote object
 * @typedef {Object} module:voting#vote
 * @property {number} total - the total votes for the requested entity
 * @property {(string|boolean)} voted - The direction the requesting user has voted in ('+', '-' or false)
 */
